# Planning

This folder holds **local-only** planning material that is intentionally **not committed** to git.

- **`esma_1.docx`** — the master backend backlog for ShowUp (the full 20-epic, multi-month roadmap:
  foundation, auth, profiles, check-ins, PostGIS location, matching, date lifecycle, show-up score,
  payments, notifications, analytics, safety/moderation, remote-config/CMS, localization, privacy,
  observability, Redis/BullMQ, testing/CI, admin, deployment). It is the source of truth for what
  the backend must eventually do.

`esma_1.docx` is git-ignored (see the repo `.gitignore`). Keep the canonical copy in Jira / shared
drive; this is a working local reference only.
